# WorkFinder
WorkFinder – A Docker‑based full‑stack application that uses FastAPI (Python) for the backend, React + Vite (TypeScript) for the frontend, and PostgreSQL + Redis for data storage and caching. It enables quick job search and management with modern UI, API, and containerized deployment.
